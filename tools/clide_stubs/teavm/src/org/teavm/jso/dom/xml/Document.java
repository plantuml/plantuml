package org.teavm.jso.dom.xml;

import org.teavm.jso.JSObject;

/**
 * Compile-only stub of TeaVM's {@code Document}. PlantUML's TeaVM sources
 * only ever hold a reference to one (via {@code HTMLDocument.current()}),
 * they never call a method on it directly, so no members are needed beyond
 * the JSObject marker (see clide_stubs/teavm/README.md).
 */
public interface Document extends JSObject {
}
