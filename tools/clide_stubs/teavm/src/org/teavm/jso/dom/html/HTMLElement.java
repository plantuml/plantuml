package org.teavm.jso.dom.html;

import org.teavm.jso.dom.xml.Element;

/**
 * Compile-only stub of TeaVM's {@code HTMLElement}. Extends {@link Element}
 * only - PlantUML's TeaVM sources call {@code setTextContent(...)} on an
 * HTMLElement, which this inherits; no extra members needed
 * (see clide_stubs/teavm/README.md).
 */
public interface HTMLElement extends Element {
}
