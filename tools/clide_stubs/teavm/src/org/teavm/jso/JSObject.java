package org.teavm.jso;

/**
 * Compile-only stub of TeaVM's {@code JSObject} - the marker interface every
 * JS-backed type extends. Deliberately empty: this jar only exists to let
 * clide/jdtls compile PlantUML's TeaVM-specific sources, never to run them
 * (see clide_stubs/teavm/README.md).
 */
public interface JSObject {
}
